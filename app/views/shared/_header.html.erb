<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="container-fluid">
    <a class="navbar-brand" href="/">RUNTEQ 中間試験</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <form class="d-flex">
      <% if @q.present? %>
      <%= search_form_for @q, url: posts_path do |f| %>       
        <%= f.search_field :title_or_body_cont, id: 'search_field_of_post', class: 'form-control', placeholder: '投稿', value: params.dig(:q, :title_or_body_cont) %>
        <%= f.search_field :body_cont, id: 'search_field_of_comment', class: 'form-control', placeholder: 'コメント', value: params.dig(:q, :body_cont) %>
        <%= f.search_field :user_profile_name_cont, id: 'search_field_of_user', class: 'form-control', placeholder: 'ユーザー', value: params.dig(:q, :user_profile_name_cont) %>
        <br>
        <%= f.submit '検索' %>
      <% end %>
      <% end %>
      </form>
      <ul class="navbar-nav mb-2 mb-lg-0 ms-auto">
        <% if current_user.present? %>
          <li class="nav-item">
            <%= link_to '投稿一覧', posts_path, class: 'nav-link' %>
          </li>
          <li class="nav-item">
            <%= link_to '投稿作成', new_post_path, class: 'nav-link' %>
          </li>
          <li class="nav-item">
            <%= link_to 'ログアウト', logout_path, class: 'nav-link', method: :delete, data: { confirm: 'ログアウトします' } %>
          </li>
        <% else %>
          <li class="nav-item">
            <%= link_to 'ユーザー登録', sign_up_path, class: 'nav-link' %>
          </li>
          <li class="nav-item">
            <%= link_to 'ログイン', login_path, class: 'nav-link' %>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
